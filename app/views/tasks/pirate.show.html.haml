= render :partial => 'shared/nav_bar'
%h1
  The Task: #{@task.name}
%h3
  Details: #{@task.description}
  %br
  #{@task.worth} Doubloons

= button_to 'Accept Quest', pirate_adds_path(current_user, @task)
= link_to "Back", pirate_tasks_path(current_user)

#task_verify_message
- tasks.first(Task::MAX).each do |task|
  %ul.task
    %li #{link_to task.name, captain_task_path(current_user, task)}
    %li #{task.pirate.username}'s Quest
    %li #{task.worth} Doubloons
    = link_to 'Confirm Complete?', captain_confirms_path(current_user, task), :remote => true, :class => 'tiny button alert round', :id => 'verified_task'

- treasure_board.each do |treasure|
  .large-3.columns.treasure_item
    = image_tag("/treasure_chest.png")
    %br

    .cost #{number_with_delimiter(treasure.total_price, :delimiter => ',')} Doubloons (+tax)
    = link_to "Loot", pirate_treasure_path(current_user, treasure), :remote => true, :id => 'buy_treasure', :method => :put
  .large-1.columns
